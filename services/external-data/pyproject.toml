[project]
name = "external-data"
version = "0.1.0"
description = "Module 04 — real provider adapters with provenance, cache and quota"
requires-python = "==3.12.*"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.34.0",
    "pydantic==2.10.4",
    "pydantic-settings==2.7.1",
    "httpx==0.28.1",
    "tenacity==9.0.0",
    "structlog==24.4.0",
    "prometheus-client==0.21.1",
    "sqlalchemy==2.0.36",
    "asyncpg==0.30.0",
    "alembic==1.14.0",
    "redis==5.2.1",
    "pyyaml==6.0.2",
    # GTFS-Realtime arrives as Protocol Buffers, not JSON. These are the
    # official generated bindings; hand-rolling a parser for a binary wire
    # format the provider can change is not a reasonable trade.
    "gtfs-realtime-bindings==1.0.0",
    # GTFS schedule times are local to the agency, and the container image is
    # slim enough to have no system zone database. Without this a New York
    # timetable is read as UTC and every train looks four hours late.
    "tzdata==2024.2",
    "opentelemetry-api==1.29.0",
    "opentelemetry-sdk==1.29.0",
    "opentelemetry-exporter-otlp-proto-http==1.29.0",
    "opentelemetry-instrumentation-fastapi==0.50b0",
    "opentelemetry-instrumentation-httpx==0.50b0",
]

[dependency-groups]
dev = [
    "pytest==8.3.4",
    "pytest-asyncio==0.25.0",
    "respx==0.22.0",
    "ruff==0.8.6",
    "mypy==1.14.1",
    "types-PyYAML==6.0.12.20241230",
    # Phase 7: validate what this module actually emits against the frozen
    # contract in packages/contracts. Twice now the schema and the producer
    # have drifted apart and nobody found out until someone validated by hand.
    "jsonschema==4.23.0",
    "referencing==0.35.1",
    "pytest-cov==6.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "ASYNC", "S", "T20", "RUF"]
ignore = ["S101"]  # assert is fine in tests

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S105", "S106"]
"app/migrations/*" = ["I001"]
"scripts/*" = ["T201", "S310"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
plugins = ["pydantic.mypy"]
exclude = ["app/migrations/versions/"]

[[tool.mypy.overrides]]
# google.transit ships generated protobuf modules with no py.typed marker.
module = ["prometheus_client.*", "opentelemetry.*", "google.transit.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
# Canary tests hit real providers, so they stay out of the default run.
addopts = "-q --strict-markers -m 'not canary'"
markers = [
    "canary: hits a real provider over the network; excluded from the default run",
]
