# Copy to .env (git-ignored). Never commit real values.
DISCORD_BOT_TOKEN=            # Developer Portal > Bot > Reset Token
GUILD_ID=                     # Discord: Developer Mode > right-click server > Copy Server ID
